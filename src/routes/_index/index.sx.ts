import { styled } from "@mui/material";

export const Index = styled("div")({
  alignItems: "center",
  backgroundColor: "#080",
  color: "#080",
  display: "flex",
  flex: 2,
  justifyContent: "center",
});
